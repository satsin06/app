import 'package:flutter/material.dart';

class LoginView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    Map<String, dynamic> demo = {"id": 1};
    return Scaffold(
      appBar: AppBar(
        title: Text('Login'),
      ),
    );
  }
}
